#!/bin/bash
echo AKEYLESS_AUTH_JWT=$TFC_WORKLOAD_IDENTITY_TOKEN >> $TFC_AGENT_ENV

if ![ -z "$AKEYLESS_DEBUG" ]; then
    echo TF_VAR_AKEYLESS_AUTH_JWT_DEBUG=$TFC_WORKLOAD_IDENTITY_TOKEN >> $TFC_AGENT_ENV
fi
